{
  "name": "kaizen_debate_club",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "python3 -m http.server 3000 -d public",
    "build": "mkdir -p build && cp -R public/. build/",
    "sync-gallery": "python3 scripts/sync_gallery.py"
  }
}
